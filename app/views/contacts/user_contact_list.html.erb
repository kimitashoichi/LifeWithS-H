<div class="container">
<h1 class="contact-all-title">Contacts ALL</h1>

<% if @users_contacts.present? %>
   <div class="col-lg-10 col-lg-offset-1">
      <table class="table">
      	<thead>
      		<th class="table-title">Title</th>
          <th></th>
      		<th class="table-title">Post date</th>
          <th></th>
          <th></th>
      	</thead>

     <% @users_contacts.each do |c| %>
      	<thead>
      		<th><%= c.contact_title %></th>
          <th></th>
          <th><%= c.created_at.strftime("%Y.%m.%d") %></th>
      		<th><%= link_to '詳細', contact_path(c.id), :class =>"cp_btn", :style=>"color:black;" %></th>
          <th><%= link_to 'Delete',contact_path(c.id), method: :delete, :class =>"cp_btn", :style=>"color:black;", data: {confirm: '本当に削除しますか？'} %></th>
      	</thead>
     <% end %>
    </table>
  </div>
<% else %>
  <h3 class="contacts-none-message">お問い合わせはまだありません</h3>
  <% end %>
</div>